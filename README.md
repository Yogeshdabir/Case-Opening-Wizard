# Case Opening Wizard

Python/Tkinter utility for case-opening workflow automation.

## Requirements

- Python 3
- Selenium (`pip install -r requirements.txt`)

## Run

```bash
python case_opening_wizard.py
```

The supplied archive also contained generated PyInstaller build output. Generated binaries and build caches were not committed to keep the repository lightweight.
